name: sqlite3_compress
version: 0.1.0

authors:
  - Paul M. Lambert <plambert@plambert.net>

targets:
  test:
    main: src/test.cr

dependencies:
  sqlite3:
    github: crystal-lang/crystal-sqlite3
  sqlite3_uri:
    github: plambert/sqlite3_uri.cr

crystal: '>= 1.16.3'

license: MIT
